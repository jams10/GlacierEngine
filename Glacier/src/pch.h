#pragma once

// 미리 컴파일 된 헤더
// 자주 바뀌지 않는, 거의 바뀔 여지가 없는, 많은 코드로 작성된, 그리고 여러 곳에서 사용될 여지가 있는 헤더 파일들(표준 템플릿 등)을 미리 컴파일해 사용하기 위한 헤더.

#include <iostream>
#include <memory>
#include <utility>
#include <algorithm>
#include <functional>

#include <string>
#include <sstream>
#include <vector>
#include <unordered_map>
#include <unordered_set>

#include "Glacier/Log.h"

